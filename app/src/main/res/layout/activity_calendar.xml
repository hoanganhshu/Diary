<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools">
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F6FF"
    tools:context="com.diary.app.demo.ui.calendar.CalendarActivity">

    <!-- ===== HEADER CARD (Custom gradient, nổi bật) ===== -->
    <androidx.cardview.widget.CardView
        android:id="@+id/calendarHeaderCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="12dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="20dp"
        app:cardElevation="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_calendar_header"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingTop="16dp"
            android:paddingBottom="12dp">

            <!-- Hàng trên: Back + Tiêu đề + Today -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/btnBack"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@android:color/transparent"
                    android:src="@drawable/arrow_back"
                    android:tint="#FFFFFF" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:fontFamily="@font/nunito_semibold"
                    android:gravity="center"
                    android:text="@string/calendar"
                    android:textColor="#FFFFFF"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/btnSelectMonthYear"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:gravity="center"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:fontFamily="@font/nunito_semibold"
                    android:text="@string/today"
                    android:textColor="#FFFFFF"
                    android:textSize="13sp"
                    app:strokeColor="#FFFFFF"
                    app:strokeWidth="1dp"
                    app:cornerRadius="16dp"
                    app:backgroundTint="@android:color/transparent" />

            </LinearLayout>

            <!-- Hàng điều hướng tháng -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/btnPrevMonth"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="Tháng trước"
                    android:src="@drawable/mui_ten"
                    android:rotation="180" />

                <TextView
                    android:id="@+id/tvMonthYear"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:fontFamily="@font/nunito_semibold"
                    android:gravity="center"
                    android:textColor="#FFFFFF"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="Tháng 5, 2026" />

                <ImageButton
                    android:id="@+id/btnNextMonth"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="Tháng sau"
                    android:src="@drawable/mui_ten" />

            </LinearLayout>

            <!-- Hàng tên thứ trong tuần -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="horizontal">

                <TextView style="@style/CalDayHeader" android:text="CN" />
                <TextView style="@style/CalDayHeader" android:text="T2" />
                <TextView style="@style/CalDayHeader" android:text="T3" />
                <TextView style="@style/CalDayHeader" android:text="T4" />
                <TextView style="@style/CalDayHeader" android:text="T5" />
                <TextView style="@style/CalDayHeader" android:text="T6" />
                <TextView style="@style/CalDayHeader" android:text="T7" />

            </LinearLayout>

        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- ===== LƯỚI NGÀY ===== -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCalendarDays"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="4dp"
        android:background="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/calendarHeaderCard" />

    <!-- ===== DANH SÁCH NHẬT KÝ ===== -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvDiariesCalendar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rvCalendarDays"
        tools:listitem="@layout/item_diary" />

    <!-- Ảnh "không có nhật ký" -->
    <ImageView
        android:id="@+id/no_diary"
        android:layout_width="246dp"
        android:layout_height="142dp"
        android:background="@drawable/empty_diary"
        android:layout_marginTop="10dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rvCalendarDays" />

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>